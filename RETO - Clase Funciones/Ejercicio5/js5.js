let numeros = []
let ingresar = parseInt(prompt("Ingresar números"))
var total

function agregar(numero) {
  if (numeros <= 20) {

  }else if (numeros) {
    alert("")
  }

  for (let index = 0; index < numeros.length; index++) {
      suma + Math.random()
  }
  var total = numeros + numeros;
  return numeros;
}

console.log(numeros);



// if (numeros <= 30 ) {
        
// }
// for (let index = 1; index < n.length; index++) {
//    suma += array[index]; 
// }
// return numeros (suma + Math.random());